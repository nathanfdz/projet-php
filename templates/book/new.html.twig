{% extends 'base.html.twig' %}

{% block title %}New Book{% endblock %}

{% block body %}
    <h1>Create new Book</h1>

    {{ include('book/_form.html.twig') }}

    <a href="{{ path('app_book_index') }}">back to list</a>
    <a class="toggle-theme"
       href="{{ path('app_theme_set', {'theme': app.request.cookies.get('theme') == 'dark' ? 'light' : 'dark'}) }}">
        Passer en mode {{ app.request.cookies.get('theme') == 'dark' ? 'clair' : 'sombre' }}
    </a>

{% endblock %}
